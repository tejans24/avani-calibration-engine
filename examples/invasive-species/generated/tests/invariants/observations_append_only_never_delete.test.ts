import { describe, test } from 'vitest';

// Invariant: Records are never deleted, only superseded with a correction flag.
describe('observations_append_only_never_delete', () => {
  test.todo('holds: Records are never deleted, only superseded with a correction flag.');
});
