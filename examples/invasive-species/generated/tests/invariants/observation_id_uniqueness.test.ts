import { describe, test } from 'vitest';

// Invariant: Every observation carries a globally unique id (offline-sync idempotency).
describe('observation_id_uniqueness', () => {
  test.todo('holds: Every observation carries a globally unique id (offline-sync idempotency).');
});
